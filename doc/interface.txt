Global:
	String get(String filepath) Gets the contents of the file specified at path
		relative to /jslibs/.

Graph:
	Boolean isEdgeBetween(Node from, Node to)
	Edge edgeBetween(Node from, Node to)
	Object hashGraph()
	Node nodeWithName(String name)
	Array nodes()
	Array shortestPath(Node from, Node to)
	Array randomWalk(Node from, Node to)
	String toString()

Edge:
	Boolean isEdgeBetween(Node from, Node to)
	String toString()

Node:
	iterate
	Array adjacents() - Get all adjacent edges.
	jsval get(String key)
	void set(String key, jsval value)
	String name()
	Edge addAcyclicEdge(
	Edge addCyclicEdge
	Edge addEdge
	Boolean isLeaf
	Boolean isConnected(Node a, Node b)
	Boolean hasAdjacentNode(String name)
	int edgeCount()
	String toString()

Generator:
	String generate()
	static Generator create(jsval base, function generation)

Math:
	float random()
	void seedrandom(jsval seed)